# 售票系統深化 Roadmap

## 目標與定位

本專案的重點不是持續增加一般 CRUD，而是完成一套能證明「高併發售票如何避免超賣、如何維持訂單與庫存一致性」的系統。

核心展示能力：

- Spring Boot、Spring Security、MyBatis、PostgreSQL 與 Redis 整合
- 訂位、付款、取消及逾期釋放的完整交易流程
- 同一座位只能由一筆有效訂單持有
- API 冪等、狀態機、併發測試與可觀測性
- Vue 3 管理後台與 WebSocket 即時通知

## 狀態標記

| 標記 | 意義 |
| --- | --- |
| ✅ 已完成 | 已有可運作實作，仍可能需要測試或重構 |
| 🟡 部分完成 | 已有基礎功能，但尚未達成完整驗收條件 |
| ⬜ 待辦 | 尚未實作 |

## 專案現況

| 領域 | 狀態 | 現況 |
| --- | --- | --- |
| 會員與驗證 | ✅ | 註冊、登入、登出、JWT Access/Refresh Token、Redis Token 管理 |
| 活動與場次 | ✅ | 活動查詢、管理員新增／修改活動與建立場次 |
| 訂票流程 | 🟡 | 已有訂位、模擬付款、取消與逾期；仍需強化一致性與冪等 |
| 選座 | 🟡 | 已有座位圖與不可用座位查詢；尚缺資料庫級唯一約束及即時同步 |
| 即時通知 | ✅ | 已有 STOMP WebSocket 通知 |
| 管理後台 | 🟡 | 已有基礎頁面；儀表板、細緻權限與稽核尚未完成 |
| 測試與 CI | ⬜ | 僅有少量測試，尚未建立完整自動化驗證 |
| 部署環境 | ✅ | Docker Compose 整合前端、後端、PostgreSQL 與 Redis |

## Phase 0：技術基線整理

目標：先消除會妨礙後續擴充的技術債。

- [ ] 所有原始碼、SQL 與 Markdown 統一使用 UTF-8。
- [ ] `date`、`time` 等字串欄位遷移為 `date`、`time` 或 `timestamptz`。
- [ ] Token 統一由 `Authorization: Bearer <token>` 傳入，不放在 Request DTO。
- [ ] API 使用明確 Request／Response DTO，逐步移除 `Map<String, Object>`。
- [ ] 統一 REST 路徑、HTTP 狀態碼、錯誤代碼及全域例外格式。
- [ ] 使用 Flyway 或 Liquibase 管理 Schema 與種子資料版本。
- [ ] 為外鍵、常用查詢欄位及唯一性需求建立約束與索引。

完成條件：全新資料庫可由 migration 建立；API 不再從 body 接收 Token；文件與測試輸出沒有亂碼。

## Phase 1：訂票核心與一致性（最高優先）

### 1.1 庫存與座位模型

- [ ] 將座位建模為場次座位，而非僅以字串保存在訂單中。
- [ ] 建立 `(session_id, seat_id)` 的有效占用唯一規則。
- [ ] 定義庫存不變條件：數量不得為負，且 `reserved + sold <= capacity`。
- [ ] 訂位成功時於同一交易內建立訂單並增加保留數。
- [ ] 付款、取消與逾期時於同一交易內更新訂單及庫存。

### 1.2 訂單狀態機

```text
PENDING_PAYMENT ──付款──> PAID ──退款──> REFUNDED
        │
        ├──使用者取消──> CANCELLED
        └──付款逾期────> EXPIRED
```

- [x] 已有五種訂單狀態及付款期限欄位。
- [ ] 將狀態轉換集中於單一 Domain Service，禁止任意覆寫狀態。
- [ ] SQL 更新包含預期舊狀態，並檢查 affected rows。
- [ ] 已付款、取消或逾期訂單不得再次付款。
- [ ] 重複付款、取消及逾期任務必須得到一致結果。

### 1.3 併發控制與冪等

- [ ] 選定並記錄庫存策略：PostgreSQL 條件更新／列鎖；Redis 僅作加速或短期占位。
- [ ] 同一座位的並發請求只能有一筆成功。
- [ ] 建立訂單支援 `Idempotency-Key`，避免重送造成重複訂單。
- [ ] 付款回呼以平台交易編號建立唯一約束。
- [ ] 排程逾期與付款競爭時，只允許一種最終狀態成立。
- [ ] 交易提交後才發送通知與安排後續工作。

### 1.4 訂單編號

- [x] 訂單編號由後端與 PostgreSQL 流水號產生。
- [x] 資料庫具有訂單編號唯一限制。
- [ ] 補上跨日、並發及序號邊界測試。

### Phase 1 驗收條件

- 100 個並發請求搶同一座位，恰好一筆成功。
- 任意流程結束後，庫存不為負數且不超賣。
- 重送付款或取消請求不會重複異動庫存。
- 應用程式重啟後，未付款訂單仍能正確逾期釋放。

## Phase 2：測試與品質保證

- [ ] Service 單元測試：狀態轉換、價格計算、權限判斷。
- [ ] Controller 整合測試：驗證、輸入錯誤與 HTTP 回應。
- [ ] Spring Security 測試：匿名、會員、主辦方及管理員權限。
- [ ] 使用 Testcontainers 啟動 PostgreSQL 與 Redis。
- [ ] 交易整合測試：訂位、付款、取消、逾期與 rollback。
- [ ] 併發測試：同座位競爭、不同座位並行及庫存邊界。
- [ ] 前端元件測試與購票流程 E2E 測試。
- [ ] GitHub Actions 執行後端測試、前端測試及建置。

完成條件：Pull Request 必須通過建置與測試；關鍵流程具有正常、錯誤、邊界與競爭案例。

## Phase 3：強化管理後台

詳細規格見 [PHASE3_ADMIN_DESIGN.md](PHASE3_ADMIN_DESIGN.md)。

- [ ] 銷售儀表板：營收、售票率、庫存及訂單狀態。
- [ ] 活動生命週期：草稿、上架、下架與封存。
- [ ] 場次、票種、分區定價及限購管理。
- [ ] 系統管理員、活動主辦方、驗票人員與會員 RBAC。
- [ ] 主辦方及驗票人員的資料範圍限制。
- [ ] 後台重要操作稽核紀錄。
- [ ] CSV 報表匯出。

## Phase 4：電子票與驗票

- [ ] 付款成功後產生不可預測的電子票 Token 與 QR Code。
- [ ] QR Code 不直接暴露會員、訂單或座位資料。
- [ ] 建立工作人員驗票頁面及場次指派。
- [ ] 使用條件更新防止重複入場。
- [ ] 保存驗票時間、工作人員、裝置與撤銷紀錄。

## Phase 5：付款與退款

- [ ] 串接一個付款平台的測試環境。
- [ ] 驗證付款回呼簽章及金額、幣別、訂單編號。
- [ ] 不信任前端傳入的付款狀態或實付金額。
- [ ] 保存付款事件及回呼摘要，以供除錯與稽核。
- [ ] 回呼、查單及退款 API 皆具冪等性。
- [ ] 建立退款狀態與營收沖銷規則。

## Phase 6：可觀測性與高流量保護

- [ ] Spring Boot Actuator 健康檢查與 readiness／liveness。
- [ ] 每個請求加入 correlation ID，使用結構化日誌。
- [ ] 監控延遲、錯誤率、訂位成功率、庫存衝突及逾期數量。
- [ ] 對登入、訂位、付款及驗票 API 加入 Rate Limiting。
- [ ] 熱門活動加入虛擬排隊室及短效入場憑證。
- [ ] 建立負載測試報告，記錄吞吐量、P95/P99 延遲與錯誤率。
- [ ] 建立備份、還原及故障演練流程。

## 建議實作順序

1. 完成 Phase 0 的資料型別、API 驗證與 migration。
2. 修正訂位、付款、取消與逾期的交易及狀態機。
3. 加入唯一約束、冪等機制與併發測試。
4. 建立 Testcontainers 與 CI，持續驗證核心規則。
5. 完成 Phase 3 管理後台及稽核紀錄。
6. 實作 QR Code 驗票，再串接第三方付款。
7. 最後加入監控、限流、排隊室與正式負載測試。

## 作品集完成定義

- 高併發下不重複售出座位、不超賣。
- 付款、取消及逾期競爭仍保持訂單與庫存一致。
- 所有金額由後端計算，所有重要操作可追蹤。
- 權限同時限制「能做什麼」與「能操作哪些資料」。
- 一個指令可建立環境，CI 可重現並驗證核心流程。
