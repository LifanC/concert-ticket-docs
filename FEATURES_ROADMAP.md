# 售票系統深化 Roadmap

最後整理：2026-09-11。實作狀態依目前原始碼整理；測試結果引用 [2026-09-10 測試紀錄](TEST_REPORT.md)，本次文件更新未重新執行測試。

## 目標與定位

本專案的重點不是持續增加一般 CRUD，而是完成一套能證明「高併發售票如何避免超賣、如何維持訂單與庫存一致性」的系統。

核心展示能力：

- Spring Boot、Spring Security、MyBatis、PostgreSQL 與 Redis 整合
- 訂位、付款、取消及逾期釋放的完整交易流程
- 同一座位只能由一筆有效訂單持有
- API 冪等、狀態機、併發測試與可觀測性
- Vue 3 管理後台與 WebSocket 即時通知

## 狀態標記

| 標記 | 意義 |
| --- | --- |
| ✅ 已完成 | 已有可運作實作，仍可能需要測試或重構 |
| 🟡 部分完成 | 已有基礎功能，但尚未達成完整驗收條件 |
| ⬜ 待辦 | 尚未實作 |

功能實作與驗收分開追蹤：功能已有程式碼，不代表已通過真實資料庫交易、併發或完整 E2E 驗收。測試與驗收清單中的未勾選項目，也包含已有部分案例但尚未完整驗證的工作；完成時應附上可重跑案例與結果。

## 專案現況

| 領域 | 狀態 | 現況 |
| --- | --- | --- |
| 會員與驗證 | ✅ | 註冊、登入、登出、JWT Access/Refresh Token、Redis Token 管理 |
| 活動與場次 | ✅ | 活動查詢、管理員新增／修改活動與建立場次 |
| 訂票流程 | 🟡 | 已完成交易、集中狀態轉換、座位唯一占用、建立訂單冪等與逾期補償；退款與完整驗收仍待完成 |
| 選座 | ✅ | 已有場次座位與資料庫唯一約束，查詢依場次隔離；即時座位同步仍待完成 |
| 即時通知 | ✅ | 已有 STOMP WebSocket 通知 |
| 管理後台 | 🟡 | 已有基礎頁面；儀表板、細緻權限與稽核尚未完成 |
| 測試與 CI | 🟡 | 已有 JUnit／Mockito、部分 Security、Playwright 與 Python 測試及前端建置紀錄；真實 PostgreSQL 交易／併發、完整購票 E2E 與 CI 待完成 |
| 部署環境 | ✅ | Docker Compose 整合前端、後端、PostgreSQL 與 Redis |

## Phase 0：技術基線整理

目標：先消除會妨礙後續擴充的技術債。

- [x] 所有原始碼、SQL 與 Markdown 統一使用 UTF-8。
- [x] `date`、`time` 等字串欄位遷移為 `date`、`time` 或 `timestamptz`。
- [x] Token 統一由 `Authorization: Bearer <token>` 傳入，不放在 Request DTO。
- [x] API 使用明確 Request DTO。
- [ ] 完成 REST API 一致性檢查：目前仍有 `saveTicket`、`dopayprice` 等動作式路徑；整理路徑、HTTP 狀態碼、錯誤代碼及全域例外格式，變更時同步更新前端與文件。
- [x] 提供開發用 `db-init` 初始化 SQL；資料表與約束見 [db-init/README.md](db-init/README.md)。
- [ ] 建立版本化 migration 與既有資料庫升級流程，驗證升級後保留資料及約束。
- [x] 為外鍵、常用查詢欄位及唯一性需求建立約束與索引。

完成條件：全新資料庫可由 migration 建立；API 不再從 body 接收 Token；文件與測試輸出沒有亂碼。

## Phase 1：訂票核心與一致性（最高優先）

### 1.1 庫存與座位模型

- [x] 將座位建模為場次座位，而非僅以字串保存在訂單中。
- [x] 建立 `(session_id, seat_id)` 的有效占用唯一規則。
- [x] 定義庫存不變條件：數量不得為負，且 `reserved + sold <= capacity`。（訂票 SQL 與 `db-init` Check Constraint 雙重保護。）
- [x] 訂位成功時於同一交易內建立訂單並增加保留數。
- [x] 付款、取消與逾期時於同一交易內更新訂單及庫存。

### 1.2 訂單狀態機

```text
PENDING_PAYMENT ──付款──> PAID ──退款──> REFUNDED
        │
        ├──使用者取消──> CANCELLED
        └──付款逾期────> EXPIRED
```

退款箭頭為規劃流程；目前僅有 `REFUNDED` 狀態定義，尚未實作退款操作。

- [x] 已有五種訂單狀態及付款期限欄位。
- [x] 將狀態轉換集中於單一 Domain Service，禁止任意覆寫狀態。（目前付款／取消／逾期由 `BookingOrderService` 處理；退款流程仍待 Phase 5。）
- [x] SQL 更新包含預期舊狀態，並檢查 affected rows。
- [x] 已付款、取消或逾期訂單不得再次付款。
- [x] 重複付款、取消及逾期任務必須得到一致結果。（庫存只異動一次；重複付款／取消回傳 409，重複逾期不再異動。）

### 1.3 併發控制與冪等

- [x] 選定並記錄庫存策略：PostgreSQL 條件更新／列鎖；Redis 僅作加速或短期占位。（目前庫存由 PostgreSQL 條件更新與交易保護；Redis 未參與庫存判定。）
- [ ] 驗證同一座位的並發請求只能有一筆成功。（已有交易與唯一約束保護；需補上可重跑的真實 PostgreSQL 100 個並發請求測試。）
- [x] 建立訂單支援 `Idempotency-Key`，避免重送造成重複訂單。
- [ ] 付款回呼以平台交易編號建立唯一約束。（隨 Phase 5 金流串接實作。）
- [ ] 驗證排程逾期與付款競爭時，只允許一種最終狀態成立。（已有列鎖與條件更新；目前 mock 測試未驗證真實資料庫競爭。）
- [x] 定期掃描逾期待付款訂單，不依賴記憶體排程。（已有補償掃描實作；完整 JVM 重啟與資料庫釋放驗收仍待補上。）
- [x] 交易提交後才發送通知與安排後續工作。

### 1.4 訂單編號

- [x] 訂單編號由後端與 PostgreSQL 流水號產生。
- [x] 資料庫具有訂單編號唯一限制。
- [ ] 補上跨日、並發及序號邊界測試。

### Phase 1 驗收條件

- [ ] 100 個並發請求搶同一座位，恰好一筆成功。
- [ ] 任意流程結束後，庫存不為負數且不超賣。
- [ ] 重送付款或取消請求不會重複異動庫存。
- [ ] 應用程式重啟後，未付款訂單仍能正確逾期釋放。

## Phase 2：測試與品質保證

### 測試策略與優先順序

測試範圍依功能風險決定，不因個人開發而省略核心單元測試與整合測試。先驗證「不超賣、訂單與庫存一致」，再驗證系統能承受多少流量；不要求每個方法都寫單元測試。

| 測試類型 | 驗證重點 | 本專案情境 |
| --- | --- | --- |
| Unit Test（單元測試） | 個別業務規則是否正確 | 訂單狀態轉換、拒絕非法轉換，例如已付款訂單不可變成逾期 |
| Integration Test（整合測試） | 元件、SQL 與交易一起運作是否正確 | 訂位、付款、取消、逾期同步更新訂單與座位／庫存；中途失敗時完整 rollback |
| Concurrency Test（併發測試） | 同時操作時是否仍維持資料正確 | 同座位只能一人訂位成功、付款與逾期競爭、相同下單請求重送 |
| Load Test（負載測試） | 預期流量下是否達到效能目標 | 模擬活動查詢與購票流量，量測吞吐量、P95/P99 延遲及錯誤率 |
| Stress Test（壓力測試） | 超出負荷時的瓶頸、失敗行為與恢復能力 | 逐步增加流量，觀察資源飽和、逾時，以及降載後是否恢復 |

整合測試就是 Integration Test；手動在網頁完成一次購票流程較接近手動端到端（E2E）測試，無法充分涵蓋交易失敗、邊界條件及競爭情境。併發測試與整合測試可以重疊，例如連接真實 PostgreSQL 驗證多個請求的鎖定與交易；併發正確性也不必等到高流量才驗證。

個人開發的執行優先順序：

1. 核心整合測試：驗證訂位、付款、取消、逾期的資料一致性與失敗回滾。
2. 關鍵併發測試：驗證同座位競爭、付款與逾期競爭、冪等請求重送，並檢查最終資料庫狀態。
3. 少量單元測試：集中覆蓋重要業務規則與邊界條件。
4. 負載測試：先定義環境、資料量、流量模型與效能門檻，再量測預期流量。
5. 壓力測試：超出預期負荷，確認瓶頸、資料一致性及降載後恢復能力。

### 測試待辦與既有紀錄

2026-09-10 紀錄：Java 18 項（Spring 啟動／權限 5 項、訂單 Service 13 項）、Microsoft Edge 9 項、Python 6 項測試通過，前端建置成功。另有實際建立訂單與重新查詢紀錄，但尚未涵蓋付款、取消及逾期的完整購票 E2E。Java 訂單測試使用 mock Mapper，Python 聚合測試使用 SQLite，不能據此認定 PostgreSQL 交易或併發驗收完成。詳細結果與限制見 [測試紀錄](TEST_REPORT.md)。

- [x] 訂單 Service 單元測試：付款／取消／逾期狀態轉換、拒絕重複操作與庫存異常（mock Mapper）。
- [ ] 補齊 Service 測試：價格計算與權限判斷。
- [ ] 補齊 Controller 整合測試：輸入驗證、錯誤格式與 HTTP 回應；目前已有部分權限回應案例。
- [ ] 補齊 Spring Security 測試：已有匿名、會員及管理員部分案例；JWT 驗證、不同會員資料隔離及未來主辦方權限仍待完成。
- [ ] 使用 Testcontainers 啟動 PostgreSQL 與 Redis（目前測試原始碼與 Maven 設定未包含 Testcontainers）。
- [ ] 交易整合測試：以真實 PostgreSQL 驗證訂位、付款、取消、逾期與 rollback。
- [ ] 併發測試：以真實 PostgreSQL 驗證同座位競爭、不同座位並行、冪等重送及庫存邊界。
- [ ] 同毫秒併發請求（Same-millisecond Requests）：以同步屏障集中釋放多個訂位請求，記錄伺服器端實際到達時間，確認是否落在同一毫秒；驗證同座位僅一筆成功、不同訂單編號不重複，且庫存不超賣。
- [ ] 資料庫悲觀鎖／樂觀鎖驗證（DB Locking）：使用真實 PostgreSQL 與獨立交易，分別驗證悲觀鎖的等待、逾時及提交／回滾後釋放，以及樂觀鎖的版本或預期舊值條件更新與 affected rows 衝突判定；確認競爭失敗不會覆寫資料或重複異動庫存。
- [x] 部分 Playwright 瀏覽器測試：活動搜尋／收藏、登入導向、會員與管理員查詢、選座確認流程。
- [ ] 前端元件測試與完整購票 E2E：建立訂單後的付款、取消、逾期與畫面狀態更新。
- [x] Python 銷售報表測試：聚合規則、金額精度與 CSV 輸出；已有真實 PostgreSQL 匯出紀錄。
- [ ] GitHub Actions 執行後端測試、前端測試及建置。

## Phase 3：強化管理後台

目前已有活動／場次／訂單查詢、活動維護與建立場次；以下為後續擴充範圍，詳細設計文件待建立。

- [ ] 銷售儀表板：營收、售票率、庫存及訂單狀態。
- [ ] 活動生命週期：草稿、上架、下架與封存。
- [ ] 場次、票種、分區定價及限購管理。
- [ ] 系統管理員、活動主辦方、驗票人員與會員 RBAC。
- [ ] 主辦方及驗票人員的資料範圍限制。
- [ ] 後台重要操作稽核紀錄。
- [x] Python 匯出各場次已付款訂單數與金額的 CSV，由 Java 定期執行；尚未整合後台圖表或下載介面。

## Phase 4：電子票與驗票

- [ ] 付款成功後產生不可預測的電子票 Token 與 QR Code。
- [ ] QR Code 不直接暴露會員、訂單或座位資料。
- [ ] 建立工作人員驗票頁面及場次指派。
- [ ] 使用條件更新防止重複入場。
- [ ] 保存驗票時間、工作人員、裝置與撤銷紀錄。

## Phase 5：付款與退款

- [ ] 串接一個付款平台的測試環境。
- [ ] 驗證付款回呼簽章及金額、幣別、訂單編號。
- [ ] 不信任前端傳入的付款狀態或實付金額。
- [ ] 保存付款事件及回呼摘要，以供除錯與稽核。
- [ ] 回呼、查單及退款 API 皆具冪等性。
- [ ] 建立退款狀態與營收沖銷規則。

## Phase 6：可觀測性與高流量保護

- [ ] Spring Boot Actuator 健康檢查與 readiness／liveness。
- [ ] 每個請求加入 correlation ID，使用結構化日誌。
- [ ] 監控延遲、錯誤率、訂位成功率、庫存衝突及逾期數量。
- [ ] 對登入、訂位、付款及驗票 API 加入 Rate Limiting。
- [ ] 熱門活動加入虛擬排隊室及短效入場憑證。
- [ ] 建立負載測試報告，記錄吞吐量、P95/P99 延遲與錯誤率。
- [ ] 負載測試前定義執行環境、資料量、流量模型、測試時長及通過門檻；區分預期的售罄／座位衝突與非預期系統錯誤。
- [ ] 建立壓力測試報告：逐步加壓至超出負荷，記錄 CPU、記憶體、資料庫連線池與鎖等待等瓶頸，確認降載後可恢復。
- [ ] 負載與壓力測試結束後檢查訂單、座位及庫存不變條件，確認沒有超賣、負庫存或重複異動。
- [ ] 建立備份、還原及故障演練流程。

## 建議實作順序

以下列出接下來的工作優先順序；完成狀態統一維護於各 Phase，避免重複清單不同步。

1. 建立 Testcontainers PostgreSQL 測試，先驗證既有訂位、付款、取消、逾期交易與失敗回滾，再補同座位競爭及冪等重送。
2. 完成 100 個並發請求、付款與逾期競爭、訂單編號邊界及完整 JVM 重啟補償驗收；發現問題時修正核心流程。
3. 將既有測試與前端建置納入 CI，補齊完整購票 E2E、權限隔離及版本化 migration。
4. 完成管理後台銷售儀表板、資料範圍權限與稽核紀錄。
5. 實作電子票與驗票，再串接第三方付款測試環境及退款；整合付款成功產生電子票與退款撤銷票券的流程。
6. 補上監控與限流，在定義環境及門檻後執行負載／壓力測試，再依量測結果決定排隊室需求。

## 作品集展示驗收標準

- [ ] 不超賣、不重複售位：多人同時搶同一座位，最後只能有一筆有效訂單。
- [ ] 訂單與庫存一致：付款、取消、逾期同時發生，也不會重複扣庫存或釋放已售出的座位。
- [ ] 金額正確、操作可追蹤：票價由後端決定，不能靠修改前端價格買便宜票；重要操作有紀錄可查。
- [ ] 權限與資料隔離：會員只能看自己的訂單，主辦方只能管理自己的活動。
- [ ] 環境與測試可重現：別人可以依文件啟動專案，CI 能自動檢查核心功能。
