# 售票系統功能延伸建議

## 專案現況

目前專案已具備以下基礎功能：

- 會員註冊、登入、登出與個人資料管理
- JWT Access Token／Refresh Token 驗證
- Redis Token 管理
- 活動與場次管理
- 訂票、模擬付款及取消訂單
- 管理員後台
- WebSocket 即時通知
- PostgreSQL、Redis 與 Docker Compose 環境

後續可依照以下階段逐步擴充，使專案更接近正式售票平台。

## 第一階段：完善售票核心

### 1. 庫存鎖定與防止超賣

- 使用者建立訂單時，暫時保留票券或座位。
- 設定付款期限，例如 10 分鐘。
- 逾時未付款時，自動取消訂單並釋放庫存。
- 使用 Redis 原子操作、分散式鎖或資料庫鎖處理併發購票。
- 增加併發測試，確認剩餘票數不會變成負數。

### 2. 後端產生訂單編號

目前訂單編號由前端依訂單數量產生，多人同時訂票時可能產生重複編號。建議改由後端使用以下方式之一產生：

- UUID
- PostgreSQL Sequence
- 日期加資料庫流水號
- Snowflake ID

同時應在資料庫保留唯一限制，避免重複訂單編號。

### 3. 完整訂單狀態流程

建議使用固定代碼或 Enum 狀態：ticket_status

| 狀態 | 說明 |
| --- | --- |
| `PENDING_PAYMENT` | 等待付款 |
| `PAID` | 已付款 |
| `CANCELLED` | 取消 |
| `EXPIRED` | 超過付款期限 |
| `REFUNDED` | 已退款 |

建議使用固定代碼或 Enum 狀態：activity_category

| 狀態 | 說明 |
| --- | --- |
| `MUSIC_CONCERT` | 音樂演唱會 |
| `STAGE_PLAY` | 舞台劇 |
| `SPECIAL_EXHIBITION` | 展覽特展 |

建議使用固定代碼或 Enum 狀態：activity_status

| 狀態 | 說明 |
| --- | --- |
| `COMING_SOON` | 即將開賣 |
| `TICKETS_ARE_ON_SALE` | 售票中 |
| `ENDED` | 已結束 |

狀態：ticket
| 狀態 | 說明 |
| --- | --- |
| `expires_at` | 付款到期時間 |
| `paid_at` | 實際付款時間 |
| `cancelled_at` | 取消時間 |

後端應檢查狀態轉換，例如已付款訂單不能再次付款，已取消訂單不能恢復付款。

### 4. 第三方付款整合

- 串接綠界、藍新或 Stripe 測試環境。
- 由付款平台回呼確認付款結果，不直接信任前端傳入的付款狀態。
- 驗證回呼簽章。
- 使用冪等機制避免重複回呼造成重複扣庫存。
- 支援取消付款與退款紀錄。

## 第二階段：改善使用者體驗

### 5. 劃位選座

- 建立場館、區域、排數及座號資料。
- 前端顯示互動式座位圖。
- 區分可選、暫時保留、已售出及不可使用的座位。
- 使用 WebSocket 即時同步座位狀態。
- 不同區域可以設定不同票價。

### 6. 電子票券與驗票

- 付款完成後產生 QR Code 電子票。
- QR Code 使用不可預測且可驗證的票券代碼。
- 建立管理員或工作人員驗票頁面。
- 防止同一張票重複入場。
- 保存驗票時間、驗票人員及驗票裝置等紀錄。

### 7. 活動搜尋與篩選

- 依活動名稱搜尋。
- 依分類、日期、場館及價格區間篩選。
- 支援分頁及排序。
- 顯示熱門、即將開賣及即將售完的活動。
- 加入收藏活動功能。

### 8. 通知中心

- 訂單建立通知。
- 付款成功通知。
- 付款期限即將到期提醒。
- 活動延期、場次異動或取消通知。
- 開賣提醒及候補成功通知。
- 在既有 WebSocket 之外加入 Email 通知。

## 第三階段：強化管理後台

### 9. 銷售儀表板

- 活動及場次售票率。
- 即時營收統計。
- 付款、取消與退款數量。
- 剩餘票券或座位數量。
- 每日、每週及每月銷售趨勢。
- CSV 或 Excel 報表匯出。

### 10. 完整活動管理

- 活動封面與介紹圖片上傳。
- 草稿、上架、下架及售完狀態。
- 多票種與分區定價。
- 每人限購張數。
- 早鳥票、會員預購與優惠碼。
- 活動內容及場次異動紀錄。

### 11. 角色與權限管理

建議將權限拆分為：

- 系統管理員
- 活動主辦方
- 驗票人員
- 一般會員

主辦方只能管理自己建立的活動；驗票人員只能存取驗票功能。後台的重要操作應保存稽核紀錄，包括操作者、操作內容、時間及结果。

## 第四階段：提升系統穩定性

### 12. 排隊與限流機制

- 熱門活動開賣時提供虛擬排隊室。
- 使用 Redis 保存排隊順序及入場資格。
- 限制每位會員及 IP 的請求頻率。
- 防止重複送單、惡意搶票與機器人操作。
- 對購票 API 加入 Rate Limiting。

### 13. 自動化測試

- Service 單元測試。
- Controller API 整合測試。
- Spring Security 權限測試。
- 使用 Testcontainers 測試 PostgreSQL 與 Redis。
- 訂票併發及超賣壓力測試。
- 前端元件測試與端對端測試。

### 14. 可觀測性與部署

- Spring Boot Actuator 健康檢查。
- 結構化日誌及請求追蹤 ID。
- API 錯誤率、回應時間及庫存指標監控。
- 使用 GitHub Actions 自動執行測試與建置。
- 建立開發、測試及正式環境設定。
- 使用 Flyway 或 Liquibase 管理資料庫版本。

## 建議先改善的技術問題

在擴充新功能前，建議先處理以下基礎問題：

1. 將原始碼、SQL 與 Markdown 文件統一為 UTF-8，修復目前的中文亂碼。
2. 日期欄位使用 PostgreSQL 的 `date`、`time` 或 `timestamp`，避免以字串保存。
3. 為活動、場次、訂單及使用者建立正式外鍵關聯。
4. 狀態與權限使用固定代碼或 Enum，不直接以顯示文字作為資料值。
5. Token 統一由 `Authorization: Bearer <token>` Header 傳送，不放在每一個 Request DTO。
6. 為常用查詢欄位建立索引，並補上必要的唯一限制。
7. 訂票、付款、取消及庫存異動使用資料庫交易。
8. API 回傳使用明確 DTO，減少使用 `Map<String, Object>`。
9. 統一 REST API 命名及 HTTP 狀態碼。
10. 增加輸入驗證、全域錯誤代碼與安全日誌。

## 推薦實作順序

若目標是將本專案作為履歷或面試作品，建議依以下順序實作：

1. 修復 UTF-8 亂碼與資料庫欄位型別。
2. 將訂單編號改由後端產生。
3. 完成庫存鎖定、付款期限及防超賣機制。
4. 建立清楚的訂單狀態流程。
5. 加入 QR Code 電子票及驗票功能。
6. 建立後台銷售儀表板。
7. 補齊整合測試、併發測試及 CI 流程。
8. 視需求加入第三方付款、劃位選座及虛擬排隊室。

這條路線可以完整呈現 Spring Boot、Spring Security、PostgreSQL、Redis、交易一致性、WebSocket、Vue 3 與 Docker 的整合能力。

init.sql
-- 範例活動與場次資料（僅首次初始化資料庫時建立）
INSERT INTO interviewworks_ticket.activity
    (id, "name", category, "date", venue, status, price, description)
VALUES
    ('ACT-2026-001', '經典舞台劇之夜', 'STAGE_PLAY', '2026-09-05', '國家戲劇院', 'ENDED', 1080, '年度經典舞台劇特別場。'),
    ('ACT-2026-002', '夏日星光音樂祭', 'MUSIC_CONCERT', '2026-09-20', '台北流行音樂中心', 'COMING_SOON', 1280, '戶外舞台演出，包含多組音樂人。'),
    ('ACT-2026-003', '經典舞台劇之夜', 'STAGE_PLAY', '2026-10-05', '國家戲劇院', 'TICKETS_ARE_ON_SALE', 1680, '年度經典舞台劇特別場。')
ON CONFLICT (id) DO NOTHING;

INSERT INTO interviewworks_ticket.session
    (id, activity_id, "date", "time", salesdate, salestime, capacity, sold)
VALUES
    ('S-001', 'ACT-2026-001', '2026-09-01', '19:30', '2026-08-15', '12:00', 180, 0),
    ('S-002', 'ACT-2026-002', '2026-09-20', '19:30', '2026-08-15', '12:00', 180, 0),
    ('S-003', 'ACT-2026-002', '2026-09-21', '19:30', '2026-08-15', '12:00', 118, 0),
    ('S-004', 'ACT-2026-003', '2026-10-05', '14:30', '2026-09-01', '12:00', 80, 0)
ON CONFLICT (id) DO NOTHING;