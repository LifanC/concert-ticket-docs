# 第二階段：改善使用者體驗設計方案

## 1. 文件目的

本文件依據 `FEATURES_ROADMAP.md` 的第二階段內容，規劃以下四項功能：

1. 電子票券與驗票
2. 活動搜尋、篩選與收藏
3. 通知中心
4. 劃位選座

本階段的目標是讓使用者能更容易找到活動、完成購票、取得電子票並收到重要通知，同時提供工作人員安全且可追蹤的驗票流程。

## 2. 建議實作順序

| 順序 | 功能 | 原因 |
| --- | --- | --- |
| 1 | 電子票券與驗票 | 範圍明確，可完整呈現付款後到入場的流程 |
| 2 | 搜尋、篩選與收藏 | 開發風險較低，可直接改善活動探索體驗 |
| 3 | 通知中心 | 可串接訂單、活動、收藏及開賣提醒 |
| 4 | 劃位選座 | 涉及即時庫存、併發控制與付款期限，複雜度最高 |

在開始第二階段前，應先確認第一階段的訂單狀態、庫存鎖定、付款期限及防止超賣機制已經穩定。

---

## 3. 電子票券與驗票

### 3.1 使用者目標

- 付款成功後可立即取得電子票。
- 可在「我的訂單」中隨時查看票券。
- 一筆訂單有多張票時，每張票都能獨立驗票。
- 能清楚辨識票券目前是否可使用。

### 3.2 電子票券頁面

每張電子票建議顯示：

- QR Code
- 活動名稱
- 活動日期與時間
- 場館
- 場次
- 區域、排數及座號（劃位活動）
- 票種及票價
- 訂單編號
- 票券狀態
- 入場注意事項

可提供以下操作：

- 查看票券
- 下載票券
- 加入行動裝置錢包（後續功能）

### 3.3 票券狀態

| 狀態 | 說明 | 畫面呈現 |
| --- | --- | --- |
| `VALID` | 可以入場 | 綠色「可使用」 |
| `USED` | 已完成驗票 | 灰色「已使用」並顯示驗票時間 |
| `CANCELLED` | 訂單已取消 | 紅色「已取消」 |
| `REFUNDED` | 已退款 | 紅色「已退款」 |
| `EXPIRED` | 票券已失效 | 灰色「已失效」 |

票券狀態應由後端根據訂單、付款、退款及驗票紀錄決定，前端只負責顯示。

### 3.4 QR Code 安全設計

- 不直接放入訂單編號、使用者 ID 或可推測的流水號。
- 使用不可預測的隨機 Token。
- 後端只保存 Token 的雜湊值，降低資料外洩風險。
- QR Code 僅代表票券識別資料，不代表前端可以自行判定有效。
- 驗票時必須連線至後端確認狀態。
- Token 應可撤銷，退款或取消後立即失效。

### 3.5 驗票工作人員流程

1. 工作人員登入驗票頁面。
2. 選擇被授權的活動及場次。
3. 開啟相機掃描 QR Code。
4. 前端將 Token 送至後端驗證。
5. 後端確認票券、場次及使用狀態。
6. 後端以原子操作將票券標記為已使用。
7. 畫面顯示結果並提供聲音或震動回饋。

### 3.6 驗票結果

| 結果 | 顏色 | 說明 |
| --- | --- | --- |
| 驗票成功 | 綠色 | 顯示票券及座位摘要 |
| 已使用 | 橘色 | 顯示首次驗票時間 |
| 票券無效 | 紅色 | Token 不存在或驗證失敗 |
| 已取消或退款 | 灰色 | 該票券不可入場 |
| 場次不符 | 黃色 | 有效票券，但不是目前場次 |

### 3.7 驗票紀錄

每次驗票應保存：

- 票券 ID
- 活動與場次 ID
- 驗票結果
- 驗票時間
- 驗票人員 ID
- 驗票裝置識別資料
- 必要時保存來源 IP

同一張票被不同裝置同時掃描時，只允許其中一次成功。

### 3.8 建議 API

| 方法 | 路徑 | 用途 |
| --- | --- | --- |
| `GET` | `/api/tickets/{ticketId}` | 取得電子票資訊 |
| `GET` | `/api/orders/{orderId}/tickets` | 取得訂單內所有票券 |
| `POST` | `/api/check-in/validate` | 驗證並使用票券 |
| `GET` | `/api/check-in/sessions/{sessionId}/records` | 查詢場次驗票紀錄 |

### 3.9 驗收標準

- 只有已付款訂單會產生可用票券。
- 多張票具有不同 QR Code Token。
- 第一次掃描成功，第二次掃描顯示已使用。
- 退款或取消後無法驗票。
- 驗票人員只能存取被授權的場次。
- 所有驗票動作都有可追蹤紀錄。

---

## 4. 活動搜尋、篩選與收藏

### 4.1 活動列表頁

頁面包含：

- 關鍵字搜尋列
- 分類篩選
- 日期區間
- 場館篩選
- 價格區間
- 活動狀態
- 是否仍有票
- 排序選項
- 分頁

手機版可將篩選條件放入抽屜或底部彈出面板。

### 4.2 排序方式

- 最近舉辦
- 最新上架
- 價格由低至高
- 價格由高至低
- 熱門程度

### 4.3 活動卡片

每張活動卡片顯示：

- 活動封面
- 活動名稱
- 日期與場館
- 最低票價
- 活動狀態
- 即將售完標籤
- 收藏按鈕

### 4.4 首頁推薦區塊

- 熱門活動
- 即將開賣
- 售票中
- 即將售完
- 使用者收藏

「熱門」可依近期瀏覽數、收藏數與銷售量加權計算。「即將售完」可先以剩餘票數低於總容量 10% 作為判定條件，比例應做成可調整設定。

### 4.5 搜尋體驗

- 關鍵字比對活動名稱，可視需求延伸至描述及場館。
- 輸入搜尋文字時使用約 300 至 500 毫秒 debounce。
- 篩選條件保存於 URL Query String。
- 重新整理、上一頁或分享網址後仍保留條件。
- 沒有搜尋結果時顯示原因及「清除篩選」按鈕。
- 後端負責分頁、篩選及排序，不在前端載入全部活動後處理。

### 4.6 收藏流程

- 已登入使用者可收藏或取消收藏活動。
- 未登入使用者點擊收藏時導向登入。
- 登入成功後返回原活動頁面。
- 同一會員不可重複收藏同一活動。
- 收藏的即將開賣活動可開啟開賣提醒。

### 4.7 建議 API

| 方法 | 路徑 | 用途 |
| --- | --- | --- |
| `GET` | `/api/activities` | 搜尋、篩選、排序及分頁 |
| `GET` | `/api/activities/featured` | 取得首頁推薦活動 |
| `GET` | `/api/users/me/favorites` | 取得我的收藏 |
| `POST` | `/api/activities/{activityId}/favorite` | 收藏活動 |
| `DELETE` | `/api/activities/{activityId}/favorite` | 取消收藏 |

搜尋參數範例：

```text
GET /api/activities?keyword=音樂&category=MUSIC_CONCERT&venue=台北&dateFrom=2026-09-01&dateTo=2026-09-30&minPrice=500&maxPrice=2000&sort=date,asc&page=0&size=12
```

### 4.8 驗收標準

- 可組合多個篩選條件。
- 搜尋結果、總筆數及分頁資料正確。
- URL 能保存目前查詢狀態。
- 同一活動不會被重複收藏。
- 未登入收藏完成登入後能返回原頁面。
- 空結果、載入中及載入失敗都有明確畫面。

---

## 5. 通知中心

### 5.1 通知類型

| 類型 | 觸發時機 | 建議管道 |
| --- | --- | --- |
| 訂單建立 | 成功建立待付款訂單 | 站內、WebSocket |
| 付款成功 | 後端確認付款完成 | 站內、WebSocket、Email |
| 付款即將到期 | 到期前指定時間 | 站內、WebSocket、Email |
| 訂單逾期 | 訂單自動失效 | 站內、WebSocket |
| 活動延期 | 管理員變更活動日期 | 站內、WebSocket、Email |
| 場次異動 | 場次時間或場館變更 | 站內、WebSocket、Email |
| 活動取消 | 管理員取消活動 | 站內、WebSocket、Email |
| 開賣提醒 | 收藏活動開始售票前 | 站內、WebSocket、Email |
| 候補成功 | 使用者取得購票資格 | 站內、WebSocket、Email |

### 5.2 通知資料

每則通知包含：

- 通知 ID
- 使用者 ID
- 通知類型
- 標題
- 簡短內容
- 對應活動、場次或訂單 ID
- 點擊後導向路徑
- 建立時間
- 已讀時間
- Email 傳送狀態

### 5.3 前端設計

- 導覽列顯示鈴鐺圖示。
- 鈴鐺旁顯示未讀數量。
- 下拉選單顯示最近通知。
- 提供單則已讀與全部已讀。
- 點擊通知後前往相關活動或訂單。
- 完整通知頁支援分頁及已讀／未讀篩選。

### 5.4 通知架構

訂單或活動狀態改變時，先產生通知事件，再由通知服務處理：

```text
訂單／活動狀態改變
        ↓
建立通知事件
        ↓
保存站內通知
        ↓
WebSocket 即時推送
        ↓
視通知類型寄送 Email
```

通知失敗不應回滾已成功的付款或訂單交易。Email 發送應支援重試及錯誤紀錄，並避免同一事件重複寄送。

### 5.5 通知偏好

使用者可設定是否接收：

- 行銷與推薦通知
- 收藏活動開賣提醒
- 一般活動提醒

付款結果、訂單到期及活動取消等交易型重要通知，不建議允許完全關閉。

### 5.6 建議 API

| 方法 | 路徑 | 用途 |
| --- | --- | --- |
| `GET` | `/api/notifications` | 取得通知列表 |
| `GET` | `/api/notifications/unread-count` | 取得未讀數量 |
| `PATCH` | `/api/notifications/{notificationId}/read` | 標示單則已讀 |
| `PATCH` | `/api/notifications/read-all` | 全部標示已讀 |
| `GET` | `/api/users/me/notification-preferences` | 取得通知偏好 |
| `PUT` | `/api/users/me/notification-preferences` | 更新通知偏好 |

### 5.7 驗收標準

- 訂單與活動事件只建立一次對應通知。
- 在線使用者能即時收到 WebSocket 通知。
- 離線後重新登入仍可看到未讀通知。
- 點擊通知可到達正確頁面。
- Email 暫時失敗不影響主要交易。
- Email 重試不會造成無限重複寄送。

---

## 6. 劃位選座

### 6.1 資料概念

建議區分以下資料：

```text
場館 Venue
└── 區域 Section
    └── 排 Row
        └── 座位 Seat

活動 Activity
└── 場次 Session
    ├── 場次區域與票價 Session Section Price
    └── 場次座位 Session Seat
```

場館座位是可重複使用的模板；每一個活動場次應建立自己的場次座位狀態，避免不同場次共用庫存。

### 6.2 座位狀態

| 狀態 | 說明 |
| --- | --- |
| `AVAILABLE` | 可選擇 |
| `HELD` | 已被暫時保留 |
| `SOLD` | 已付款售出 |
| `UNAVAILABLE` | 不開放、故障或視線不良 |

`SELECTED` 僅為目前使用者瀏覽器中的畫面狀態，不應保存為正式庫存狀態。只有後端回傳保留成功後，座位才進入 `HELD`。

### 6.3 使用者流程

1. 選擇活動與場次。
2. 開啟互動式座位圖。
3. 選擇一個或多個座位。
4. 前端向後端申請暫時保留座位。
5. 後端以原子操作確認座位仍可用並建立保留紀錄。
6. 保留成功後建立待付款訂單並開始倒數。
7. 付款成功後座位改為 `SOLD`。
8. 付款逾期或取消訂單後座位改回 `AVAILABLE`。

### 6.4 互動式座位圖

- 顯示舞台方向。
- 不同票價區使用不同顏色。
- 座位圖提供狀態圖例。
- 已售出及不可使用座位無法點擊。
- 桌面版支援縮放及拖曳。
- 手機版可先選區域，再查看該區座位。
- 固定顯示已選座位、票價、總額及剩餘付款時間。
- 選取超過限購數量時立即提示。
- 除顏色外，同時以圖示或形狀表示狀態，照顧色覺差異。

### 6.5 即時同步

WebSocket 可推送以下事件：

- 座位被保留
- 座位保留解除
- 座位已售出
- 座位被管理員停用

WebSocket 用於更新畫面，不作為庫存正確性的最終依據。使用者送出保留請求時，後端仍須再次驗證座位狀態。

### 6.6 併發與保留規則

- 座位保留必須是原子操作。
- 同一座位同時被多人選擇時，只允許一人成功。
- 保留紀錄包含擁有者與到期時間。
- 只有保留該座位的使用者能建立或支付相關訂單。
- 保留逾期後由排程或事件機制釋放。
- 釋放流程應具備冪等性，重複執行不會增加錯誤庫存。
- 使用者離開頁面不應立即釋放座位，以免網路中斷造成權益損失，仍以保留期限為準。

### 6.7 建議 API

| 方法 | 路徑 | 用途 |
| --- | --- | --- |
| `GET` | `/api/sessions/{sessionId}/seat-map` | 取得場次座位圖與票價 |
| `POST` | `/api/sessions/{sessionId}/seat-holds` | 暫時保留座位 |
| `GET` | `/api/seat-holds/{holdId}` | 查詢保留狀態與期限 |
| `DELETE` | `/api/seat-holds/{holdId}` | 主動放棄保留 |
| `POST` | `/api/orders` | 使用有效保留紀錄建立訂單 |

保留座位請求範例：

```json
{
  "seatIds": ["SEAT-A-01", "SEAT-A-02"]
}
```

回應應包含保留識別碼、座位資料、總額及到期時間。

### 6.8 例外情境

- 座位剛被其他人保留：提示使用者重新選位並更新座位圖。
- WebSocket 中斷：顯示連線狀態，恢復後重新同步完整座位狀態。
- 保留即將到期：在最後一分鐘明顯提醒。
- 保留已到期：停止付款並導回選位流程。
- 部分座位保留失敗：整批失敗，不建立不完整的多座位訂單。
- 付款成功但前端未收到結果：重新查詢訂單狀態，不可直接再次付款。

### 6.9 驗收標準

- 座位圖正確顯示區域、座位、狀態及票價。
- 兩位使用者同時搶同一座位時只有一位成功。
- 付款完成後座位立即變成已售出。
- 到期及取消後能正確釋放座位。
- WebSocket 斷線不會破壞庫存正確性。
- 不同場次具有彼此獨立的座位狀態。
- 前後端都會檢查每人限購張數。

---

## 7. 共用 UX 狀態

每項功能都應設計以下畫面狀態：

- 初始狀態
- 載入中
- 正常完成
- 無資料
- 輸入錯誤
- 權限不足
- 網路中斷
- 伺服器錯誤
- 資料已被其他使用者更新

重要操作應提供明確回饋，但避免只依賴短暫 Toast。付款結果、驗票結果及座位保留期限等重要資訊，應直接保留在主要畫面上。

## 8. 共用技術原則

- 前端顯示狀態不能取代後端驗證。
- 訂票、付款、座位保留及驗票需使用交易或原子操作。
- 所有時間由後端提供，並明確處理時區。
- API 使用固定錯誤代碼，前端再轉換為使用者可理解的訊息。
- WebSocket 只負責即時更新，斷線後必須能透過 REST API 重新同步。
- Email、WebSocket 等通知失敗不能破壞主要業務交易。
- 重要操作需保存稽核紀錄。
- 個人資料、票券 Token 及通知內容不可寫入不必要的日誌。

## 9. 第二階段完成定義

第二階段可在符合以下條件時視為完成：

- 使用者能搜尋、篩選、排序及收藏活動。
- 已付款訂單能產生安全且獨立的電子票。
- 驗票人員能驗票，並能阻止重複入場。
- 使用者能收到並管理站內通知。
- 重要通知能透過 Email 發送並支援失敗重試。
- 劃位活動能即時顯示座位並安全保留座位。
- 併發選位不會造成同一座位重複售出。
- 核心正常流程及主要例外流程都有自動化測試。

